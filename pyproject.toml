[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "renderkit"
dynamic = ["version"]
description = "A high-performance image and video processor for VFX workflows"

readme = "README.md"
requires-python = ">=3.13,<3.14"
license = {text = "MIT"}
authors = [
    {name = "Ahmed Hindy"}
]
keywords = ["vfx", "exr", "video", "image-processing", "conversion"]
urls = {Homepage = "https://github.com/Ahmed-Hindy/renderkit", Repository = "https://github.com/Ahmed-Hindy/renderkit"}
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    "numpy>=2.3.0,<2.4",
    "openimageio>=3.0.3.1",
    "click>=8.1.0",
    "tqdm>=4.66.0",
    "PySide6-Essentials>=6.8.0,<6.9",
    "opencolorio>=2.5.0,<2.6",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-benchmark>=4.0.0",
    "pytest-cov>=4.1.0",
    "pytest-qt>=4.2.0",
    "ruff>=0.1.0",
    "mypy>=1.7.0",
    "pre-commit>=4.6.0",
]

docs = [
    "mkdocs-material>=9.5.0",
    "mkdocstrings[python]>=0.24.0",
]

packaging = [
    "imageio-ffmpeg>=0.6.0,<0.7",
    "nuitka>=4.1.0",
    "ordered-set>=4.1.0",
    "pyinstaller>=6.0.0",
    "zstandard>=0.23.0",
]

[project.scripts]
renderkit = "renderkit.cli.main:main"


[tool.ruff]
line-length = 100
target-version = "py313"
[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "UP045",  # keep Optional[...] annotations for now
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.ruff.lint.isort]
known-first-party = ["renderkit"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--strict-config",
    "--cov=renderkit",
    "--cov-report=term-missing",
    "--cov-report=html",
]

[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
]

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true

[[tool.mypy.overrides]]
module = [
    "OpenImageIO.*",
    "opencolorio.*",
]
ignore_missing_imports = true

[tool.hatch.version]
path = "src/renderkit/__init__.py"
